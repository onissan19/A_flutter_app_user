name: a_flutter_app_tensor
description: A new Flutter project

environment:
  sdk: ">=2.18.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_lints: ^3.0.1
  fl_chart: ^0.63.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
